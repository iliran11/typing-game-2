import {} from '../../constants';
import {} from '../../types';

const initialState = {};

export default function GameReducer(state: any = initialState, action: any) {
  switch (action.type) {
  }
}
