
import {ServerStatusReducer} from '../../../types'
import {initialState} from '../serverStatus'

export default function gameRestart(state:ServerStatusReducer,action:any):ServerStatusReducer {
  return initialState
}